---
paths:
  - "skills/**"
---

You are editing a skill. Skills in this repo load into other repos' sessions,
so a change here changes agent behaviour everywhere it is installed.

Read `WRITING-RULES.md` in full before changing this file. Check the result
against its "Before committing" list, then run `./scripts/check.sh`.

`AGENTS.md` holds the invariants and says which file each harness reads.
